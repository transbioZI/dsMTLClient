% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.LassoCov.R
\name{ds.LassoCov_Train}
\alias{ds.LassoCov_Train}
\title{Training a regularization tree with the control of covariates}
\usage{
ds.LassoCov_Train(
  X = NULL,
  Y = NULL,
  type = "regress",
  nlambda = 10,
  lam_ratio = 0.01,
  lambda = NULL,
  covar = NULL,
  opts = list(init = 0, maxIter = 20, tol = 0.01, ter = 2),
  datasources = NULL,
  nDigits = 10,
  intercept = F
)
}
\arguments{
\item{X}{The design matrices of multiple cohorts}

\item{Y}{Label vectors of multiple cohorts (in case of a binary outcome classes must to be coded as -1 and 1)}

\item{type}{regression(=regress) or classification(=classify)}

\item{nlambda}{The length of lambda sequence}

\item{lam_ratio}{smallest lambda / largest lambda}

\item{lambda}{The lambda sequence}

\item{covar}{Positions of adjusting covariates in the X dataset}

\item{opts}{Options controlling the optimization procedure}

\item{datasources}{The connections of servers}

\item{nDigits}{The number of digits rounded for each number prepared for network transmission}

\item{intercept}{Use intercept(=TRUE) or non-intercept(=FALSE) model}
}
\value{
The regularization tree
}
\description{
Training a regularization tree with Lasso
}
\details{
Training a regularization tree with Lasso
}
\author{
Han Cao & Augusto Anguita-Ruiz
}
